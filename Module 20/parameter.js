//declare
function sum (a, b, c){ //inside the bracket these are the parameters, you can take as much as you want
    const sum = a + b + c;
    console.log(sum);
}

sum(10, 20, 30);    //function call using parameter values